import React from 'react';
import { IconTypes } from "./Icon.types";
declare const Icon: ({ children, size, ...rest }: IconTypes) => React.ReactElement<any, string | React.JSXElementConstructor<any>>;
export default Icon;
